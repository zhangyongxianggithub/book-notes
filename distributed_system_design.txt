间接通信：
    间接是计算机科学中的基本概念，著名的断言是：
    1.计算机科学中所有的问题都可以通过某个层次上的间接方式解决.
    2.没有通过消除某种层次上的间接方式解决不了的性能问题.
    分布式系统中的间接通信就是通过中介者通信，没有发送者与接受者的耦合。这种间接通信有2个主要特点：
    1.空间解耦，就是发送者与接受者互相不知道，自己做改变不影响别的系统；
    2.时间解耦，发送者与接收者有独立的生命周期；
    间接通信用于预期会发生改变的系统或者用作事件分发。间接通信的缺点是增加间接层带来的性能开销并且难于管理。
    
